{
  "name": "alkane-lamp",
  "version": "1.0.0",
  "description": "Fusion 360 3D Lamp Parametric Generator Scripts",
  "private": true,
  "scripts": {
    "i": "npm i",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "clean": "node -e \"const fs=require('fs'),path=require('path'); function clean(dir){if(!fs.existsSync(dir))return; fs.readdirSync(dir,{withFileTypes:true}).forEach(e=>{const p=path.join(dir,e.name); if(e.isDirectory())clean(p); else if(e.name.endsWith('.js'))fs.unlinkSync(p);});} clean('fusion/scripts');\""
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
